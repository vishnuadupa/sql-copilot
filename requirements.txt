# Local dev / evaluation deps (app deployment uses requirements-app.txt,
# training on Colab uses requirements-train.txt — kept separate because
# unsloth needs its own transformers/trl/torch versions that conflict
# with pins here)
torch
transformers
peft
fastapi==0.104.1
uvicorn==0.24.0
gradio==4.15.0
groq==0.4.2
pandas==2.1.3
pyyaml==6.0.1
sqlparse==0.4.4
datasets
